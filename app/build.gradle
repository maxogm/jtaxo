apply plugin: 'com.android.application'

android {
    signingConfigs {
    }
    compileSdkVersion 15
    buildToolsVersion '20.0.0'
    defaultConfig {
        applicationId "taxo.jmax.in.rs.jmaxtaxo"
        minSdkVersion 15
        targetSdkVersion 15
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
}
